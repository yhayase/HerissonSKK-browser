# SKK 辞書登録・再帰辞書登録 状態遷移仕様書

本ドキュメントは、SKK（特に標準実装である DDSKK `~/tmp/ddskk/skk.el` の `skk-henkan-in-minibuff`）の仕様に準拠した、辞書登録モードおよび再帰辞書登録モードの厳密な状態遷移とキー操作仕様を定義します。

実装（Implementation）とテスト（Tests）は、本仕様書に定義された要件番号に基づいて作成されなければなりません。**実装の現行コードに基づいてテストを作成してはならず、本仕様を満たすようにテストと実装を同期させます。**

---

## 1. 状態および用語の定義

| 状態名 / 用語 | 定義 |
| :--- | :--- |
| **`KakuteiMode`** | 確定入力状態。入力された仮名は即座にバッファへ確定挿入される。 |
| **`MidashigoMode`** | 見出し語入力状態（`▽`）。平仮名・片仮名・送り仮名の見出し語を編集中。 |
| **`InlineHenkanMode`** | インライン単独候補選択状態（`▼`）。第 1〜3 候補をインライン表示。 |
| **`MenuHenkanMode`** | 複数候補一覧選択状態。ラベル（`a`〜`g` など）によるページネーション表示。 |
| **`RegistrationMode`** | 辞書登録モード。Shadow DOM 内の専用入力モーダル（`<input type="text">`）で登録語を編集する。 |
| **再帰深度 (`depth`)** | 辞書登録の入れ子レベル。最上位（親）は `depth = 1`。登録編集中にさらに未登録語の変換に入った場合は `depth >= 2`（再帰辞書登録ダイアログがスタックされる）。 |
| **ミニバッファ (`RegistrationModal`)** | 実 Web ページを汚染せず、Shadow DOM 内に描画される専用の `<input type="text">` モーダルダイアログ。ブラウザ標準のカーソル移動・選択・コピペ・xremap マッピングをネイティブに活用しつつ、内部で SKK 変換を行う。 |

---

## 2. 送りあり見出し語の生成規則 (`SPEC-OKURI-01`)

DDSKK [`skk.el:L2363-L2373`](file:///home/hayase/tmp/ddskk/skk.el#L2363-L2373)（`skk-compute-henkan-key2`）に準拠します。

1. **内部辞書検索・保存キー (`dictionaryKey`)**:
   - 送りなし: `見出し語`（例: `にほん`）
   - 送りあり: `語幹 + 送りアルファベット`（例: `うs`、`かt`）
2. **UI 表示用見出し語 (`displayMidashigo`)**:
   - 送りなし: `見出し語`（例: `にほん`）
   - 送りあり: `dictionaryKey` の末尾アルファベット（`[a-z]+$`）を除去し、`語幹 + "*" + 送り仮名` とする。
     - 入力 `USi` → 語幹 `う`, 送りアルファベット `s`, 送り仮名 `し` → **`う*し`** （`うs*し` や `かt*つ` は不可）
     - 入力 `Kat` → 語幹 `か`, 送りアルファベット `t`, 送り仮名 `つ` → **`か*つ`**
3. **プロンプトヘッダ表示**:
   - `depth = 1`（最上位）: `[見出し] ` または `[辞書登録] 見出し: `（HUD 表示: `[う*し] `）
   - `depth >= 2`（再帰時）: `depth` に応じてブラケットが多重化されるか、再帰登録バッジとともに `[う*し] ` を表示。

---

## 3. キー入力と状態遷移仕様

### 3.1. Enter (`RET`) による確定と中断復帰 (`SPEC-KEY-ENTER`)

- **要件 3.1.1 (非空確定)**: ミニバッファに確定文字列が存在する場合:
  - 辞書プロバイダ（個人辞書）に `dictionaryKey` と登録語（送りありの場合は語幹）を保存する。
  - `depth >= 2`（再帰時）: 親のミニバッファへ登録文字列を挿入し、親の `RegistrationMode` に復帰する。
  - `depth = 1`（ルート時）: 実 DOM エディタへ確定文字列を挿入し、元の通常モード（`KakuteiMode`）に復帰する。
- **要件 3.1.2 (空文字中断復帰)**: ミニバッファが空文字列（未入力）の状態で `Enter` が押された場合:
  - **辞書登録を中断し、直前の変換状態へ復帰する。**
  - **復帰先 A (単独候補末尾からの突入時)**:
    - 辞書登録に入る直前に表示していた単独候補（末尾候補）を再表示した `InlineHenkanMode` に復帰する。
  - **復帰先 B (候補一覧メニューからの突入時)**:
    - 辞書登録に入る直前に表示していた最後の候補リストページを表示した `MenuHenkanMode` に復帰する。
  - **復帰先 C (候補 0 件で見出し語から直接突入時)**:
    - 直前の見出し語（`▽`）を保持した状態に復帰する。
  - 再帰登録（`depth >= 2`）で空文字 Enter が押された場合は、親のミニバッファの直前の変換状態へ復帰する。

### 3.2. Ctrl+j (`skk-kakutei`) によるバッファ内確定 (`SPEC-KEY-CTRLJ`)

- **要件 3.2.1 (バッファ内変換確定)**:
  - ミニバッファ内で `▼`（候補選択中）の場合: 現在の候補をミニバッファ内の平仮名／漢字として確定固定する。
  - ミニバッファ内で `▽`（見出し語入力中）の場合: 見出し語をそのまま仮名として確定固定する。
  - 未消化ローマ字（例: 末尾の `n`）がある場合: 仮名（`ん`）に変換して確定固定する。
- **要件 3.2.2 (空文字・確定済みの無操作)**:
  - ミニバッファ内に未確定（`▼`、`▽`、ローマ字）がない場合（既に平仮名に確定されている、または空文字列の場合）:
  - **何も起きない（No-op）**。
  - **ミニバッファ（辞書登録モード）を抜けてはならない。**

### 3.3. Backspace (`DEL`, `Ctrl+h`) および Delete (`Ctrl+d`) による文字削除 (`SPEC-KEY-BS`)

- **要件 3.3.1 (カーソル直前文字の削除: Backspace / Ctrl+h)**:
  - ミニバッファ内のカーソル位置（`cursorIndex`）が `0` より大きい場合、`cursorIndex` の直前の 1 文字を削除し、`cursorIndex` を 1 つ左へ戻す。
- **要件 3.3.2 (カーソル先頭時・空バッファ時の無操作)**:
  - `cursorIndex === 0`（ミニバッファ先頭または空バッファ）の状態で `Backspace` / `Ctrl+h` が押された場合:
  - **何も起きない（No-op / エラー音相当）**。
  - **再帰登録モードを一段抜けたり、登録モードをキャンセルしてはならない。**
- **要件 3.3.3 (カーソル位置文字の削除: Delete / Ctrl+d)**:
  - `cursorIndex < buffer.length` の場合、`cursorIndex` 位置の 1 文字を削除する（`cursorIndex` 自体は移動しない）。
  - `cursorIndex === buffer.length`（末尾）の場合は **No-op** とする。

### 3.4. Ctrl+g (`keyboard-quit`) による段階的キャンセルと 1 段脱出 (`SPEC-KEY-CTRLG`)

- **要件 3.4.1 (バッファ内未確定の段階的キャンセル)**:
  - ミニバッファ内が `▼`（候補選択中）の場合: 候補選択をやめて `▽`（見出し語編集）に戻す。
  - ミニバッファ内が `▽`（見出し語入力中）の場合: 見出し語を消去して通常の平仮名入力に戻す。
  - 未消化ローマ字バッファがある場合: ローマ字バッファを消去する。
- **要件 3.4.2 (再帰登録の 1 段脱出)**:
  - ミニバッファ内に未確定文字列・ローマ字がない状態で `Ctrl+g` が押された場合:
  - `depth >= 2`（再帰時）:
    - **再帰学習を 1 段抜けて、親の `RegistrationMode` に戻る。**
    - 全体がキャンセルされて `KakuteiMode` に戻ってはならない。
  - `depth = 1`（最上位時）:
    - 辞書登録を中止し、未確定状態を破棄して元の入力状態（`KakuteiMode`）へ復帰する。

### 3.5. ミニバッファ内のカーソル移動および任意位置編集 (`SPEC-KEY-CURSOR`)

ミニバッファは Shadow DOM 内の専用モーダル入力欄（`<input type="text">`）としてレンダリングされ、フォーカスが付与される。

- **要件 3.5.1 (ブラウザネイティブ編集の活用)**:
  - 左右カーソル移動（`ArrowLeft` / `ArrowRight`）、単語ジャンプ、行頭・行末移動（`Home` / `End`）、Shift 範囲選択、クリップボード操作、および xremap による OS 側キーマッピング（`Ctrl+b` → `←`、`Ctrl+f` → `→` 等）は、`<input>` 要素のネイティブキャレット機能を通じて自然に動作する。
  - キャレットはブラウザのネイティブキャレットとして視覚的に点滅・表示される。
- **要件 3.5.2 (任意位置への文字挿入・文字削除)**:
  - 文字列の挿入および `Backspace` / `Delete` は、現在のネイティブキャレット位置に対して行われる。
- **要件 3.5.3 (熟語の部分文字抽出編集ユースケース)**:
  - 例えば「東京」と入力・変換された状態で、矢印キーや `Ctrl+b` でカーソルを「東」と「京」の間に移動し、`Backspace`（または `Ctrl+h`）を押すと「東」が削除されて「京」のみが残る。この状態で `Enter` を押すと「京」のみを登録語として確定できる。
- **要件 3.5.4 (モーダル `<input>` 内での SKK 変換動作)**:
  - モーダル `<input>` には `BrowserEditorAdapter` がアタッチされ、入力欄内部で通常どおり平仮名入力・漢字変換（`▽`/`▼`）・アスキーモード（`l`）切り替え・`Ctrl+j` による平仮名復帰が行える。
- **要件 3.5.5 (フォーカスおよび選択範囲の安全な復帰)**:
  - モーダル起動時に元の Web ページ要素（`originalActiveElement`）およびキャレット位置を退避する。
  - 登録完了（Enter）またはキャンセル（ESC / Ctrl+g / 空文字 Enter）時、モーダルを閉じて `originalActiveElement` へフォーカスを復帰し、完了時は確定単語を流し込み、中断時は直前の変換状態を復元する。
- **要件 3.5.6 (再帰登録のスタック表現)**:
  - 再帰辞書登録（`depth >= 2`）時は、親モーダルの上に子モーダルをスタック表示（または階層インジケータ付きのモーダル）し、子 `<input>` にフォーカスを移す。
  - 子の確定時は親の `<input>` のキャレット位置に確定単語を挿入して親にフォーカスを戻す。
  - 子のキャンセル時は親の `<input>` に何も挿入せずフォーカスを戻す。

---

## 4. テスト要件マッピング

各テストケースは、以下のマッピングに従って `test/unit/registration-state-transitions.test.ts` に実装します。

| テスト ID | 仕様要件 | 検証内容 |
| :--- | :--- | :--- |
| `TC-OKURI-01` | `SPEC-OKURI-01` | 送りあり `USi` 入力時にヘッダが `[う*し]` となり、アルファベット `s` が含まれないこと |
| `TC-OKURI-02` | `SPEC-OKURI-01` | 送りあり `Kat` 入力時にヘッダが `[か*つ]` となり、アルファベット `t` が含まれないこと |
| `TC-ENTER-01` | `SPEC-KEY-ENTER.1` | 文字入力後の Enter で辞書登録が完了し、確定文字列が挿入されること |
| `TC-ENTER-02` | `SPEC-KEY-ENTER.2` | 空文字列 Enter で登録を抜け、直前の単独候補（末尾候補）が表示された状態に復帰すること |
| `TC-ENTER-03` | `SPEC-KEY-ENTER.2` | 候補一覧（`MenuHenkanMode`）末尾から登録に入った後、空文字 Enter で最終ページに復帰すること |
| `TC-ENTER-04` | `SPEC-KEY-ENTER.2` | 候補 0 件で見出し語（`MidashigoMode`）から直接突入後、空文字 Enter で直前の見出し語（`▽`）を保持した状態に復帰すること |
| `TC-CTRLJ-01` | `SPEC-KEY-CTRLJ.1` | ミニバッファ内の `▽`/`▼` が `Ctrl+j` でミニバッファ内確定されること |
| `TC-CTRLJ-02` | `SPEC-KEY-CTRLJ.2` | 空文字列または確定済み文字列で `Ctrl+j` を押しても何も起きず、登録モードにとどまること |
| `TC-BS-01` | `SPEC-KEY-BS.1` | ミニバッファ内のカーソル直前文字が Backspace / Ctrl+h で 1 文字削除されること |
| `TC-BS-02` | `SPEC-KEY-BS.2` | 先頭または空文字列で Backspace / Ctrl+h を押しても再帰登録モードを抜けず、親に戻らないこと |
| `TC-BS-03` | `SPEC-KEY-BS.3` | カーソル位置の文字が Delete / Ctrl+d で削除され、末尾では No-op となること |
| `TC-CTRLG-01` | `SPEC-KEY-CTRLG.1` | ミニバッファ内の `▼` が `Ctrl+g` で `▽` に戻り、`▽` が `Ctrl+g` で消去されること |
| `TC-CTRLG-02` | `SPEC-KEY-CTRLG.2` | 再帰登録（depth 2）で未確定がないときの `Ctrl+g` で、親（depth 1）に 1 段だけ戻ること |
| `TC-RECURSIVE-01`| 全般 | 再帰登録（depth 2）で単語登録完了時、親のミニバッファに挿入され、さらに親の確定で実エディタに挿入されること |
| `TC-CURSOR-01` | `SPEC-KEY-CURSOR.1` | `ArrowLeft` / `Ctrl+b` および `ArrowRight` / `Ctrl+f` でカーソルが 1 文字単位で左右に移動すること |
| `TC-CURSOR-02` | `SPEC-KEY-CURSOR.2` | `Home` / `Ctrl+a` で先頭へ、`End` / `Ctrl+e` で末尾へカーソルがジャンプすること |
| `TC-CURSOR-03` | `SPEC-KEY-CURSOR.3` | カーソルを途中に移動した状態で文字を入力したとき、カーソル位置に文字列が割り込み挿入されること |
| `TC-CURSOR-04` | `SPEC-KEY-CURSOR.4` | 熟語（例:「東京」）から 1 文字目「東」を削除して 2 文字目「京」のみを残して登録確定できること |
