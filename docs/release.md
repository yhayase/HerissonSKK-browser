# 初回公開の準備

日常試用と並行して進める公開準備を管理します。公開の到達条件は[ロードマップ](./roadmap.md)、実機の結果は[日常試用の確認記録](./daily-trial.md)を参照してください。

## 配布物

```sh
npm ci
npm run compile
npm run zip
npm run zip:firefox
```

Chrome 用 ZIP と Firefox 用 ZIP は `.output/` に生成します。Firefox 用 ZIP は未署名です。公開・更新時は公開する変更とバージョンを確定し、提出したファイルのハッシュとコミットを記録します。現在の公開候補は `0.1.2` です。`0.1.0`・`0.1.1` は署名済み試用版として使用しました。

通常ビルドと ZIP の両方から `test.html` とテンプレート画像 `wxt.svg` を除外します。試験ページは `test/browser/test.html` をローカル HTTP サーバーから配信するため、開発・E2E では引き続き利用できます。manifest の公開リソースからも `test.html` を除外しています。

2026-09 の署名済み試用版は `de2e9c6fba9b4615b020-0.0.0.xpi` です。ユーザーの報告では、この署名時にはソースコードを提出していません。現在の WXT／Vite による変換・結合・最小化はソース提出の対象です。今後は各提出版に、対応する未変換ソースと再現用ビルド手順を添付します。

## 残る準備

- 公開する変更・バージョン・コミットを確定します。
- 確定した版で型チェック、単体テスト、Chrome／Firefox E2E、Firefox の `web-ext lint` を実行します。
- Firefox 0.1.2 の署名を取得します。同じ ID（`herissonskk@yhayase`）を維持します。作者の判断により、Android の追加確認は今回のリリース条件から外しました。
- [掲載文案](./store-listing.md)の対応範囲と制約を実機結果に合わせて確定します。
- [採用済みの掲載画像](../assets/store/README.md)は準備完了です。公開版で UI が変わった場合は撮り直します。
- [プライバシーポリシー](./privacy-policy.md)の公開 URL を用意し、ストアのデータ利用・権限申告と整合させます。
- アイコンは公式アプリの配布・紹介に利用できます。別製品への流用は個別許可とし、利用条件を配布物に含めます。本体は MIT です。
- Firefox の掲載手続きと Chrome の開発者登録・提出を進めます。Chrome の登録状況は未確認です。

本体 GitHub リポジトリは公開済みです。リポジトリ・プライバシーポリシー・Issues は認証なしでアクセスできることを確認しました。

Firefox の最小バージョンは現在 `109.0` です。データ収集なしの宣言は PC の Firefox 140、Android の Firefox 142 より前では未対応として lint 警告になります。警告を消すことだけを目的に最小バージョンは引き上げず、公開時に実際の対応範囲を確認します。

## 検証記録（2026-09-21）

- Chrome／Firefox の ZIP 生成と型チェックは成功しました。
- 両 ZIP の内容と manifest を確認し、`test.html` と `wxt.svg` が含まれないことを確認しました。
- Firefox lint はエラー 0 件、警告 2 件でした。試験ページのインライン・外部スクリプトに関する警告は解消し、最小バージョンに関する警告が残っています。
- Chrome／Firefox の入力 E2E は各 5 シナリオ（input、textarea、contenteditable、Monaco、登録・学習）すべて成功しました。単体テスト全体と辞書設定 E2E は今回再実行していません。
- ストアへの再提出、実機での更新確認は未実施です。

## ブランドと素材（2026-09-22）

表示名を HerissonSKK for Chrome／HerissonSKK for Firefox に設定しました。Firefox の拡張 ID は維持しています。承認済みの透過アイコンを 16・32・48・96・128 px で組み込み、[ストア用 512 px と原本](../assets/brand/README.md)も保存しました。設定・診断・ポップアップの画面名と掲載文案も更新しました。

同梱辞書の GPL v2 本文と wanakana・textarea-caret の MIT ライセンスを配布物に追加しました。本体のライセンス確定は未完了です。

## 検証記録（2026-09-22）

- 型チェックと単体テスト 487 件が成功しました。
- Chrome／Firefox の入力 E2E 各 5 シナリオと辞書設定 E2E が成功しました。同一プロファイルでのオフライン再起動も含みます。
- 両 ZIP の表示名、透過アイコンの全サイズ、ライセンス表示の同梱、試験ページ・テンプレート画像の除外を確認しました。
- Firefox lint はエラー 0 件、最小バージョンに関する既知の警告 2 件でした。
- 既存の署名済み版からの更新、Android 実機での新アイコン表示、ストアへの提出は未実施です。バージョンは試用版の `0.0.0` のままです。

## 座標計算のライブラリ利用への復帰（2026-09-23）

textarea の座標取得を textarea-caret 3.1.0 の呼び出しへ戻しました。normal／小数の行高、Firefox での測定幅、スクロール補正、例外時の後片付けを呼び出し側で維持します。単一行 input と Monaco／contenteditable の計測は従来どおりです。MIT 表示は引き続き同梱します。

- 変更前後の座標を Chrome／Firefox 各 9 ケース（行高 3 種 × 単一行・改行・日本語折り返し）で比較し、一致しました。
- `node test/caret-coordinates.mjs` は両ブラウザー各 19 件が成功しました。行高・折り返し・縦横スクロール、入力欄の状態保持、例外時に既存 DOM を保護する後片付けも含みます。
- 型チェック、単体テスト 489 件、既存の両ブラウザー入力・辞書設定 E2E が成功しました。
- Android 実機での今回の変更の試用は未実施です。

## 生成コードのライセンス表示（2026-09-23）

WXT／browser と Vite の補助コードの MIT 表示を追加しました。両 ZIP を再生成し、README と各本文がソースの表示ファイルとバイト単位で一致することを確認しました。今回は表示ファイルと文書の変更のため、入力動作のテストは再実行していません。

## 本体ライセンスの確定（2026-09-23）

作者の判断により、本体コードと付属文書を MIT としました。LICENSE、package.json、lockfile、配布物内の本文と適用範囲の案内を追加・更新しました。第三者コードと GPL 辞書の既存条件は維持します。

## 基本辞書の初回ダウンロード（2026-09-23）

配布 ZIP に基本辞書を含めず、初回起動時に公式の S 辞書（JSON）を取得します。取得失敗は設定画面から再試行でき、成功後は再起動時にも保存済み辞書を使います。旧同梱辞書の設定・キャッシュを維持して、更新先を公式 URL に移行します。

GPL 辞書と本文は試験用として `test/fixtures/dict/` に保持します。ブラウザー拡張の ZIP には含めません。プライバシーポリシーと掲載文案を初回通信に合わせて更新しました。候補表示の性能測定は実際にダウンロードする辞書を使うため、レポート schemaVersion 3 では辞書が固定されていないことを明記します。固定辞書を使うパーサー／ストレージ検証は引き続き試験用データを使います。

検証結果:

- 型チェックと単体テスト 492 件が成功しました。初回取得の同時呼び出し、失敗後の再試行、オフライン再起動、旧同梱設定とキャッシュの移行を確認しました。
- Chrome／Firefox の入力 E2E 各 5 シナリオと辞書設定 E2E が成功しました。公式配信先からの初回取得と、保存後のオフライン再起動を含みます。
- 両 ZIP に辞書ファイル・辞書用 GPL 本文・dict の公開リソース宣言がなく、本体 MIT と依存コードの表示が残っていることを確認しました。
- 候補表示測定を両ブラウザー各 1 回実行し、初回取得後に「日本」が表示されることを確認しました。性能評価に十分なサンプル数ではなく、測定スクリプトの動作確認です。
- 移動した固定辞書を使う `npm run test:production-dictionary` も成功しました。
- 署名済み版からの Android 実機更新とストア提出は未実施です。

## 2026-09-23 の更新: リポジトリからの公式辞書の削除

公式 SKK 辞書と試験用に移動した GPL 本文を、現在のソースツリーからも削除しました。単体テストはプロジェクト独自の小さな試験データだけを使います。公式辞書を使う統合検証は実行時にダウンロードし、既定の SHA-256 を照合してプロセス内のメモリーで使用します。辞書データをリポジトリへ保存しません。この検証にはネットワーク接続が必要です。上記の「試験用辞書を保持」という記録は、この更新より前の状態です。

過去の Git コミットに含まれる辞書は履歴に残っています。共有履歴の書き換えは実施していません。

ソースツリーからの削除後、型チェック、単体テスト491件、実行時ダウンロードを使う両ブラウザーの公式辞書検証が成功しました。両ZIPを再生成し、ソースの現行ファイルとZIPに公式辞書が残っていないことを確認しました。

## ストア用素材（2026-09-23）

Chrome の変換・単語登録・辞書設定を3枚、Firefox の変換・単語登録を2枚、1280 × 800 px で撮影しました。Chrome 用の 440 × 280 px プロモーション画像も作成しました。実際の拡張機能の表示を確認し、画像サイズと Chrome 用画像の透過なしを検証しました。撮影スクリプトと専用デモページを保存しました。Firefox の設定画面は撮影 API の制限で未撮影です。ストア提出は行っていません。

## リポジトリ名と初回バージョン（2026-09-23）

GitHub リポジトリを `yhayase/HerissonSKK-browser` にリネームし、初回公開バージョンを `0.1.0` に設定しました。パッケージ名と origin URL、文書内のリンクも新しい名前に合わせました。Firefox の拡張 ID は既存の `skk-browser-extension@yhayase` を維持し、署名済み試用版からの更新に備えます。リポジトリは現在非公開で、ストア公開前に公開する予定です。公開後にポリシー・操作説明・Issues のリンクを未ログインで確認します。

型チェック・単体テスト・両ブラウザーの ZIP 生成が成功しました。ZIP 内のバージョンが `0.1.0` であること、Firefox の拡張 ID が維持され、辞書・試験ページ・テンプレート画像が含まれないことを確認しました。Firefox lint はエラー 0 件、最小バージョンに関する既知の警告 2 件です。Firefox 起動・検証スクリプトは package.json から ZIP 名を決めるよう変更し、構文チェックを実施しました。この変更後のブラウザー E2E と Android 実機更新は未実施です。

## 作業ファイルとフォルダーの整理（2026-09-23）

整理前の変更済み・未追跡ファイルとローカル Git 履歴を `.local/backups/` に退避しました。ルートの試作画像と古い検証スクリーンショットは `.local/` に移し、未使用のテンプレート部品・画像と旧 PoC エンジンを削除しました。Git の過去履歴は変更していません。

試験ページを `public/` から `test/browser/` に移し、起動・検証スクリプトの参照を更新しました。一時スクリーンショットは `.output/` に保存し、採用済み画像は `assets/`、判断根拠となる計測・監査記録は `docs/` に保持します。フォルダーの用途は README に記載しました。

整理後の型チェック、単体テスト 491 件、Chrome／Firefox の入力 E2E 各 5 シナリオと辞書設定 E2E が成功しました。Android 実機での更新確認は引き続き未実施です。

## 最新 main の統合と公開候補の検証（2026-09-23）

GitHub の main（`c75b426`）にある入力オーバーレイ改善を公開準備と統合しました。競合したテストは textarea-caret の検証と新しいオーバーレイ表示の検証を両方維持しました。リポジトリ名の表記を `HerissonSKK-browser` に変更し、npm パッケージ名は小文字の `herissonskk-browser` としています。

型チェック、単体テスト 521 件、両ブラウザーの入力・辞書設定・オーバーレイ E2E、キャレット座標検証各 19 件が成功しました。Firefox lint はエラー 0 件、既知の最小バージョン警告 2 件です。掲載画像は統合後の `0.1.0` で撮り直し、単語登録の読みが「えりそん」であることも確認しました。リポジトリ公開、ストア提出、Android 実機での更新確認は未実施です。

## AMO 審査用ソース

`python3 scripts/release/package-source.py` で package.json のバージョンに対応するソース ZIP を `.output/` に作成します。[ビルド手順](./source-build.md)がソース ZIP のルート README になります。ソース・設定・lockfile・画像・ライセンスとビルドスクリプトを含め、依存や生成済みコード、バックアップは除外します。Firefox 配布 ZIP と別の「ソースコード」欄にアップロードしてください。

2026-09-23 にソース ZIP を独立した空のディレクトリへ展開し、README のスクリプトで依存取得から再ビルドまで実行しました。既存の Firefox 提出 ZIP と、展開後の全 25 ファイルのパス・バイト内容が一致しました。ソース ZIP は 237,606 bytes です。

- 照合した Firefox ZIP SHA-256: `594640def92cddaad7a4bc5b808dbdab1c6b9636072c814b4b9c8a9c68048382`
- ソース ZIP SHA-256: `dff68697b918733b6396bebcb6b23cdb6b253a49b5ac6272c6b941a2e82f9b97`

## 公開前の Firefox 拡張 ID の変更

作者の判断により、公開用 ID を `herissonskk@yhayase` に変更しました。上記の ID 維持・試用版からの更新という記録は、この決定以前のものです。旧 ID `skk-browser-extension@yhayase` の AMO 登録への更新としては提出せず、新規アドオンとして登録します。バージョンは `0.1.0` です。登録語・学習・辞書設定の自動移行はありません。旧試用版を削除するとその保存データが失われるため、データが必要な間は削除せず、入力処理の競合を避けるため無効化します。

新 ID で両ブラウザーの ZIP と AMO ソース ZIP を再生成しました。型チェック、Firefox 入力 E2E 全 5 シナリオが成功し、独立ディレクトリでのソース再ビルドと提出 ZIP の全 25 ファイルが一致しました。Firefox lint はエラー 0 件、既知の警告 2 件です。上記の旧 ID の ZIP ハッシュに代わり、今回の提出物は次のとおりです。

- `herissonskk-browser-0.1.0-firefox.zip` SHA-256: `2c2761b636c1f1c831c32caf286c46aa2876ed3602a493c64e0bfceb79df2b34`

- `herissonskk-browser-0.1.0-sources.zip` SHA-256: `32fc53a4a7ad909ced6c130eda432dac3bbf83f55e3f2cad6836c5b95f0e2931`

取得済みの `de2e9c6fba9b4615b020-0.1.0.xpi` は manifest を確認したところ旧 ID `skk-browser-extension@yhayase` でした。新 ID の署名済みファイルは別途取得する必要があります。

## 0.1.1: Shift 境界の促音修正

`TasSi` の `sS` で生成される「っ」を語幹へ含め、「たっs」・送り仮名「し」で検索するよう修正しました。`TsukaTte` のように送り仮名内で生成される「っ」は従来どおり送り仮名に保持します。ひらがな・カタカナ・登録画面内の変換に回帰テストを追加し、修正前の失敗と修正後の成功を確認しました。型チェックと単体テスト 525 件が成功しました。Android 実機での修正版確認は未実施です。

0.1.1 の Chrome／Firefox 入力 E2E 各 5 シナリオが成功しました。Firefox lint はエラー 0 件、既知の警告 2 件です。ソース ZIP を空のディレクトリへ展開して再ビルドし、Firefox 提出 ZIP の全 25 ファイルがバイト単位で一致しました。


## 0.1.2 公開候補（2026-09-23）

署名済み 0.1.1 以後の iframe 対応、候補削除の確認表示・状態管理、日本語フォント指定、カタカナでの Ctrl+j 修正をまとめました。Firefox ID は `herissonskk@yhayase` を維持し、`herissonskk@hayase` への移行予定は取り消しました。[提出用資料](./store-submission-0.1.2.md)と[バージョンノート](./release-notes-0.1.2.md)を用意しています。

実装の基点は `51aaae2` です。公開候補ではバージョンと提出用文書を更新しました。Chrome／Firefox の配布 ZIP は各 26 ファイルで、試験ページ・公式辞書・バックアップを含まず、本体・依存ライブラリのライセンス表示を含みます。AMO ソース ZIP を独立した空ディレクトリに展開し、README の手順で npm ci から再ビルドした結果、Firefox ZIP 内の全 26 ファイルがバイト単位で一致しました。

| 配布物 | SHA-256 |
| :--- | :--- |
| `herissonskk-browser-0.1.2-chrome.zip` | `db18bf0134076dc133b79d5181cceff2f0946fcf09d76ec11a0294098aab992e` |
| `herissonskk-browser-0.1.2-firefox.zip` | `d3eeedca9a65fdcdd16c7656efe963e53dcbd99342d584c36e8e7548dab71458` |
| `herissonskk-browser-0.1.2-sources.zip` | `3bd09fd24df8b44722fb3e1cdeee8d9ad88c2c3b639e053fc4bbce32f3bd7910` |

Chrome／Firefox の基本入力 E2E 各 5 シナリオ、埋め込み文書 E2E 各 16 シナリオ、候補削除 E2E 各 12 シナリオが成功しました。Firefox lint はエラー 0 件、data_collection_permissions と最小対応バージョンに関する既知の警告 2 件です。

GitHub は非公開、Issues は有効です。公開設定の変更、ストア提出、署名済み 0.1.2 の Android 実機更新確認は未実施です。

型チェックと単体テスト全 540 件、両ブラウザーの辞書設定・同一プロファイルでのオフライン再起動、候補表示 E2E が成功しました。Firefox 辞書 E2E は非表示タブへの Ctrl+g をフォーカス保護が破棄したため当初失敗しました。テストを実ユーザーと同じ前面化・入力欄フォーカス後の操作に修正し、Chrome／Firefox とも成功しました。製品コードの変更は不要でした。

アイコンの利用条件は、公式アプリの配布・紹介には利用可、別製品への流用は個別許可と確定し、両配布 ZIP とソース ZIP に含めました。

## 署名取得へ進む判断（2026-09-23）

作者の判断により、Android での追加確認をリリース条件から外し、Firefox 0.1.2 の署名取得へ進みます。0.1.2 を Android で確認済みとするものではありません。


## Firefox 0.1.2 署名済み成果物の取得（2026-09-23）

作者が AMO から取得した `9ac06365e4c34c9683bc-0.1.2.xpi` を確認しました。

- バージョン: `0.1.2`
- 拡張 ID: `herissonskk@yhayase`
- サイズ: 121,624 bytes
- SHA-256: `b8ab830b2e17dff73f324847363ac1fff979900f843d59620d375d801113d770`
- 提出 ZIP との比較: コード・画像・ライセンス等の 25 ファイルはバイト単位で一致しました。`manifest.json` は整形だけが異なり、JSON としての内容は一致しました。追加ファイルは `META-INF/` 内の署名関連 5 ファイルだけです。
- 署名情報: PKCS#7 に含まれる証明書の CN は拡張 ID と一致し、発行者は Mozilla AMO Production Signing Service です。署名ファイルと証明書情報の確認であり、今回 Firefox にインストールして信頼チェーンを検証したものではありません。

署名済み XPI、対応する Chrome／Firefox の提出 ZIP、審査用ソース ZIP、`SHA256SUMS.txt` を `.local/releases/0.1.2/` に保存しました。署名済みファイル名は `herissonskk-browser-0.1.2-firefox-signed.xpi` です。`.local/` は Git 管理対象外で、ビルドの再実行では削除されません。対応する提出準備コミットは `1003f52` です。

Android の追加確認は作者の判断で省略しました。次は GitHub リポジトリの公開と公開 URL の確認、Firefox／Chrome のストア掲載手続きです。非掲載版の署名取得は、ストア掲載の完了を意味しません。


## GitHub 配布準備（2026-09-23）

GitHub に `v0.1.2` のドラフトリリースを作成しました。対象コミットは `1003f5289f6e6d84ce74ee31987a29aeae551547` です。署名済み Firefox XPI、Chrome／Firefox 提出 ZIP、審査用ソース ZIP、`SHA256SUMS.txt` の 5 ファイルを添付しました。配布物はローカルの `.local/releases/0.1.2/` にも保持しています。

リポジトリとリリースはまだ公開していません。リポジトリ公開後に、README・プライバシーポリシー・Issues を未ログインで確認し、ストア掲載用リンクを確定します。


## GitHub リポジトリの公開（2026-09-23）

作者の承認により `yhayase/HerissonSKK-browser` を Public に変更しました。GitHub API で `PUBLIC` と Issues 有効を確認し、認証情報を使わない HTTP リクエストで以下の URL がすべて 200 を返すことを確認しました。

- https://github.com/yhayase/HerissonSKK-browser
- https://github.com/yhayase/HerissonSKK-browser/blob/main/docs/privacy-policy.md
- https://github.com/yhayase/HerissonSKK-browser/issues

GitHub の 0.1.2 リリースはドラフトのままです。リポジトリ公開とは別に、リリース公開とストア掲載を進めます。
