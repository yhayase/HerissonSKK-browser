# SCANOSS 検査結果（2026-09-23）

## 結果

対象 110 ファイルすべてについて API の応答を受け取りました。109 ファイルは `id: none`、1 ファイルは `id: snippet` でした。ファイル単位の完全一致はありませんでした。検出された一致を確認した範囲で、新たにライセンス対応が必要な第三者実装は見つかりませんでした。

[生の応答 JSON](./scanoss-2026-09-23-results.json) と [入力ファイルの SHA-256 一覧](./scanoss-2026-09-23-inputs.json) を保存しています。入力一覧と結果のパス集合は一致し、検査終了時にも対象ファイルの内容は入力時から変わっていません。

## 検出 1 件の確認

| 項目 | 内容 |
| --- | --- |
| 対象 | wxt.config.ts、5–22 行 |
| 比較先 | qfeuilla/emnuke v0.1.1、wxt.config.ts、6–23 行 |
| API の値 | snippet、hits=2、matched=41% |
| 比較先のライセンス | MIT。API の宣言に加えて、該当タグの LICENSE 本文を取得して確認しました。 |
| 判定 | 設定の類似であり、第三者の実装を複製した根拠とは判断しません。追加のライセンス対応は不要と判断しました。 |

[比較先の設定](https://github.com/qfeuilla/emnuke/blob/v0.1.1/wxt.config.ts) と [LICENSE](https://github.com/qfeuilla/emnuke/blob/v0.1.1/LICENSE) を確認しました。共通箇所は WXT の manifest 設定、storage 権限、raw.githubusercontent.com のホスト許可、Firefox の最小バージョンとデータ収集なしの宣言です。固有の製品名、説明、拡張 ID、条件分岐は異なります。

本リポジトリでは初期コミット b48da70 に storage 権限があり、bf3f645 で Firefox 設定、365d6fa で manifestVersion による任意ホスト権限の切り替えが追加されています。設定と変更目的の対応が確認できます。API の 41% は検出器の指標であり、「コードの41%をコピーした」という意味には扱いません。

## 範囲と実行条件

- クライアント: scanoss 1.54.2。`/tmp/herisson-scanoss-venv` に一時インストールしました。
- 対象: src、entrypoints、scripts、test のコード・HTML・CSS、ルートの TypeScript 設定、public/test.html。作業中の変更を含む 110 ファイルです。
- 除外: node_modules、生成済みバンドル、ダウンロードしたブラウザー、辞書、画像、文書。依存ライブラリの監査は別の [ライセンス調査](../licenses.md) を参照してください。
- 送信先: `https://api.osskb.org/scan/direct`。
- 送信内容: ファイルハッシュ、コード断片のフィンガープリント、行番号、サイズ、相対パス。ソース本文は送信していません。ユーザーが送信を明示的に承認した後に実施しました。
- 初回の HPSM 付き要求は HTTP 403（HPSM is disabled）で拒否されました。通常のスニペット方式で再生成し、照合を完了しました。HPSM による照合は実施できていません。
- 照合設定: flags=2306（snippet ID、best-match-only の解除、GitHub full path）、threads=2、timeout=45、retry=1。スニペットの閾値はサーバー既定値です。
- 応答の engine version: 5.4.25。KB: daily 26.09.21、monthly 26.08。
- 標準 WFP の SHA-256: `123e2ff7a5e60e68c55138df12cf07a8f49badec08170dd8033ecb664b7da714`。

実行した照合コマンド:

```sh
/tmp/herisson-scanoss-venv/bin/scanoss-py scan   --wfp /tmp/herisson-source-standard.wfp   --skip-settings-file --flags 2306 --threads 2 --timeout 45 --retry 1   --output docs/audit/scanoss-2026-09-23-results.json
```

標準 WFP は、入力一覧のファイルを相対パスを維持して一時ディレクトリにコピーし、`scanoss-py fingerprint --all-extensions --skip-settings-file` で生成しました。

## 結果の限界

一致なしは、指定データベース・アルゴリズム・閾値で検出されなかったことを意味します。未収録・非公開コード、短い断片、大幅な書き換え等の複製がないことを証明するものではありません。skk-vscode からの移植については作者自身の著作物という説明を前提とし、今回一致が返らなかったことから別の結論を導いていません。
