{
  "name": "skk-browser-extension",
  "description": "SKK Input Method Extension for browsers and VS Code for Web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "wxt",
    "dev:firefox": "wxt -b firefox",
    "start": "npm run build && node scripts/launch-browser.mjs",
    "start:firefox": "node scripts/launch-firefox.mjs",
    "build": "wxt build",
    "build:firefox": "wxt build -b firefox",
    "build:firefox:zip": "wxt zip -b firefox",
    "zip": "wxt zip",
    "zip:firefox": "wxt zip -b firefox",
    "compile": "tsc --noEmit",
    "bench:dictionary": "node scripts/benchmark-dictionary.mjs",
    "test:unit": "vitest run",
    "test": "npm run build && xvfb-run -a node test/poc-verify.mjs",
    "test:firefox": "npm run build:firefox && npm run build:firefox:zip && xvfb-run -a node test/firefox-verify.mjs",
    "test:all": "npm run test && npm run test:firefox",
    "postinstall": "wxt prepare"
  },
  "devDependencies": {
    "@types/textarea-caret": "^3.0.4",
    "@types/wanakana": "^4.0.6",
    "fake-indexeddb": "^6.2.5",
    "puppeteer-core": "^25.10.0",
    "typescript": "^5.9.3",
    "vite": "^8.1.5",
    "vitest": "^5.0.0",
    "web-ext": "^10.5.0",
    "wxt": "^0.21.3"
  },
  "dependencies": {
    "textarea-caret": "^3.1.0",
    "wanakana": "^5.3.1"
  }
}
