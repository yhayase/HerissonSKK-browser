/**
 * Recursively traverses activeElement across Shadow DOM boundaries.
 */
export function getDeepActiveElement(doc: Document | null = document): Element | null {
    throw new Error("Not implemented");
}
